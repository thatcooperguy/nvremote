# NVRemote Website — Environment Variables
# Copy to .env.local and fill in your values.

# ── API ──────────────────────────────────────────────────────────────────────
# Base URL for the NVRemote control-plane API.
NEXT_PUBLIC_API_URL=https://api.nvremote.com

# ── Sentry Error Tracking ────────────────────────────────────────────────────
# Client-side DSN (shipped to the browser).
NEXT_PUBLIC_SENTRY_DSN=

# Server-side DSN (Next.js server routes and edge functions).
SENTRY_DSN=

# Auth token for uploading source maps during `next build`.
# Generate at: https://sentry.io/settings/auth-tokens/
SENTRY_AUTH_TOKEN=
