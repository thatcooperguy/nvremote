# NVRemote Desktop Client -- Environment Variables
# Copy to .env and fill in values for local development

# Signaling server URL (defaults to https://api.nvremote.com if not set)
# SIGNALING_URL=http://localhost:3000

# API base URL override (renderer process, via Vite)
# VITE_API_BASE_URL=http://localhost:3001/api

# WebSocket URL override (renderer process, via Vite)
# VITE_SOCKET_URL=http://localhost:3001
